"""Placeholder — integration tests require Docker services (MongoDB, Kafka)."""

import pytest


@pytest.mark.integration
def test_integration_placeholder() -> None:
    """Verify the integration test suite is discoverable by pytest."""
